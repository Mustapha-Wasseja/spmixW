% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_utils.R
\name{make_knw}
\alias{make_knw}
\title{Create k-Nearest-Neighbours Spatial Weight Matrix}
\usage{
make_knw(coords, k, row_normalise = TRUE)
}
\arguments{
\item{coords}{An \eqn{N \times 2} matrix of coordinates (e.g., longitude/latitude
or projected x/y).}

\item{k}{Integer: number of nearest neighbours.}

\item{row_normalise}{Logical: if \code{TRUE} (default), row-normalise the
resulting matrix.}
}
\value{
A sparse \eqn{N \times N} weight matrix. If \code{row_normalise = TRUE},
each row sums to 1.
}
\description{
Constructs a row-normalised binary spatial weight matrix based on
k-nearest neighbours from a coordinate matrix.
}
\details{
Euclidean distances are computed between all pairs of coordinates. For each
unit, the \code{k} closest neighbours receive weight 1 (before normalisation).
The matrix is generally asymmetric (i may be j's neighbour without j being
i's neighbour).
}
\examples{
set.seed(1)
coords <- cbind(runif(20), runif(20))
W <- make_knw(coords, k = 5)
dim(W)
range(Matrix::rowSums(W))  # all 1 if row-normalised

}
