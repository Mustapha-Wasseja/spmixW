% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_panel}
\alias{simulate_panel}
\title{Simulate Spatial Panel Data}
\usage{
simulate_panel(
  N,
  Time = 10L,
  W,
  gamma = NULL,
  rho = 0,
  beta,
  theta = NULL,
  sigma2 = 1,
  effects = "twoway",
  seed = NULL
)
}
\arguments{
\item{N}{Integer: number of cross-sectional units (regions).}

\item{Time}{Integer: number of time periods (use 1 for cross-sectional).}

\item{W}{A single N x N weight matrix, or a list of M weight matrices
for convex combination DGPs.}

\item{gamma}{Numeric vector of convex weights (required when \code{W} is
a list, must sum to 1). Ignored when \code{W} is a single matrix.}

\item{rho}{Numeric: spatial autoregressive parameter (default 0).}

\item{beta}{Numeric vector of true regression coefficients (length k).}

\item{theta}{Numeric vector of WX coefficients for SDM/SDEM DGPs
(default NULL). Must have same length as \code{beta} if provided.}

\item{sigma2}{Numeric: error variance (default 1).}

\item{effects}{Character: fixed-effects specification.
\code{"none"}, \code{"region"}, \code{"time"}, or \code{"twoway"}
(default \code{"twoway"}).}

\item{seed}{Integer or NULL: random seed for reproducibility.}
}
\value{
A data frame with columns:
\describe{
\item{region}{Integer region identifier (1 to N).}
\item{year}{Integer time period identifier (omitted if \code{Time = 1}).}
\item{y}{Simulated response variable.}
\item{x1, x2, ...}{Simulated predictor variables (standard normal).}
}
The data frame is sorted by time then region, ready for
\code{\link{spmodel}}.
}
\description{
Generates synthetic spatial panel data from a SAR/SDM/SEM/SDEM DGP,
returning a ready-to-use data frame for \code{\link{spmodel}}.
}
\details{
The DGP is:
\deqn{y = (I_{NT} - \rho W_c)^{-1} (X \beta + W_c X \theta + \text{FE} + \varepsilon)}
where \eqn{W_c = \sum \gamma_m W_m} (or just W if a single matrix),
\eqn{\theta} is omitted for SAR/SEM DGPs, and FE are generated as
\eqn{\mu_i = i/N} (region) and \eqn{\nu_t = t/T} (time).
}
\examples{
\donttest{
coords <- cbind(runif(80), runif(80))
W1 <- make_knw(coords, k = 4)
W2 <- make_knw(coords, k = 8)

panel <- simulate_panel(
  N = 80, T = 10,
  W = list(W1, W2),
  gamma = c(0.7, 0.3),
  rho = 0.5,
  beta = c(1, -1),
  seed = 42
)

res <- spmodel(y ~ x1 + x2, data = panel,
               W = list(geography = W1, trade = W2),
               model = "sar",
               id = "region", time = "year",
               effects = "twoway",
               ndraw = 8000, nomit = 2000)
print(res)
}

}
